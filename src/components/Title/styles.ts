import { styled } from 'styles';

export const GameTitle = styled('h1', {
  fontWeight: 'bold',
  color: '$darkOrange',
  fontSize: '2rem',
});

export const Container = styled('div', {
  marginBottom: '1rem',
  textAlign: 'center',
});
